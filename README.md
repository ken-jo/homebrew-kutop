# Homebrew Tap for kutop

```bash
brew tap ken-jo/kutop
brew install kutop

# One-shot install without a separate tap step:
brew install ken-jo/kutop/kutop
```

Formula updates are published from tagged releases of
[`ken-jo/kutop`](https://github.com/ken-jo/kutop).
